apiVersion: v1
kind: Service
metadata:
 name: nginx
spec:
  type: {{ .Values.service.type }}
  selector:
    project: roboshop
    purpose: rs-demo
    component: frontend
    tier: frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80